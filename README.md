Debian/Ubuntu init.d script to sanity check /dev/urandom, using the randomsanity.org service

TODO: create a nice .deb package so a sysadmin can just apt-get install randomsanity to configure and install.

